/* eslint-disable no-empty */
/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
import styles from "../modules/LiveBadge.module.css";
function LiveBadge() {
  return (
    <div className={styles.badge}>
      <span>Live now</span>
    </div>
  );
}

export default LiveBadge;
